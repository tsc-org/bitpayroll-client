const LOGIN = () : string => `auth/login`
const REGISTER_EMPLOYEE = () => `auth/register_employee`
const REGISTER_ORG = () => `auth/register_organisation`

const REQUEST_VERIFICATION = () => `/verification`

export default {
    LOGIN,
    REGISTER_EMPLOYEE,
    REGISTER_ORG,
    REQUEST_VERIFICATION
}